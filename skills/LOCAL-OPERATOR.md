# Local Operator Pack

Source of truth: `C:\Users\Admin\.agents\skills`.
Authoring rules: `writing-for-agents`, `skill-curator`.
Do not paste this list into global `AGENTS.md`.

| Skill | Trigger class | Outcome |
| --- | --- | --- |
| `operator-execution` | execute / implement / handle it | Tight loop to a verified result |
| `skill-curator` | add / improve / gap-check skills | New or revised `SKILL.md` that passes the quality gate |
| `prompt-forge` | better prompt / work order | Executable objective-target-constraints-proof prompt |
| `skill-sync` | sync / copy / skill not loading | Identical skill tree on the target harness |
| `completion-receipt` | done / status / wrap up | Short evidence receipt |
| `agent-eval` | score this session / agent failed | Scorecard and ranked instruction fixes |
| `session-bootstrap` | lost context / start here / orient | Five-line map, then first real action |

## Quality bar

A pack skill stays only if it still has a distinct trigger, an executable path, and a proof. See `skill-curator/references/skill-quality-gate.md`.
