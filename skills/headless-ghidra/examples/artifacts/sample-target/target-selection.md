# Target Selection: `sample-target`

## Status

- Artifact state: Registered review surface for selection planning
- Generated by:
  - `PlanTargetSelection.java` as a supported `analysis_export` script
  - Stage 3 target selection after evidence review
- Runtime contract:
  - default runtime path is `.work/ghidra-artifacts/<target-id>/target-selection.md`
  - current validation is recorded in `latest-version-validation.md`

## Intended Fixed Output Filename

- `target-selection.md`

## Selection Schema

| Selection Mode                      | Selected Target            | Candidate Kind                                                      | Frontier Reason                                 | Relationship Type                                             | Verified Parent Boundary               | Triggering Evidence          | Secondary Metrics                  | Metrics Note                       | Selection Reason        | Question To Answer      | Tie-Break Rationale     | Deviation Reason                  | Deviation Risk                    | Status                                |
| ----------------------------------- | -------------------------- | ------------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------------------------- | -------------------------------------- | ---------------------------- | ---------------------------------- | ---------------------------------- | ----------------------- | ----------------------- | ----------------------- | --------------------------------- | --------------------------------- | ------------------------------------- |
| auto_default / documented_deviation | `pending_function@address` | outer_anchor / wrapper / thunk / dispatch_helper / substantive_body | outermost anchor or child of `matched` boundary | entry_adjacent / dispatch_edge / callee / wrapper_edge / none | `pending_local_verification` or `none` | `pending_local_verification` | `incoming_refs=<n>; body_size=<n>` | metrics are secondary context only | `pending_analyst_entry` | `pending_analyst_entry` | `pending_analyst_entry` | `none` or `pending_analyst_entry` | `none` or `pending_analyst_entry` | blocked / ready / approved / complete |

## Rules

- `Selection Reason`, `Frontier Reason`, `Question To Answer`, and
  `Tie-Break Rationale` are the minimum review fields for an automatic default
  target.
- `Secondary Metrics` may remain visible in the main surface, but `Metrics Note`
  must keep them explicitly secondary.
- `Deviation Reason` and `Deviation Risk` are required only when the workflow
  records a documented deviation.
- Before a row moves from `ready` to `approved`, record the replacement
  boundary and fallback strategy for that step in `reconstruction-log.md` and
  `comparison-command-log.md`.
- Runtime generation should write to a workspace copy under
  `.work/ghidra-artifacts/<target-id>/target-selection.md`.

## Current Local Observation

This example file records the registered review surface. Local replay
validation is captured separately in `latest-version-validation.md`.
