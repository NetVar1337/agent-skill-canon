# Evidence Candidates: `sample-target`

## Status

- Artifact state: Registered review surface for candidate extraction
- Generated by:
  - `ReviewEvidenceCandidates.java` as a supported `analysis_export` script
  - Stage 2 evidence review before target selection
- Runtime contract:
  - default runtime path is `.work/ghidra-artifacts/<target-id>/evidence-candidates.md`
  - current validation is recorded in `latest-version-validation.md`

## Intended Fixed Output Filename

- `evidence-candidates.md`

## Candidate Schema

### Frontier Candidate Rows

| Frontier Eligibility          | Candidate Kind                                                      | Function Identity          | Frontier Basis                               | Relationship Type                                             | Verified Parent Boundary               | Triggering Evidence          | Secondary Metrics                  | Metric Label                       |
| ----------------------------- | ------------------------------------------------------------------- | -------------------------- | -------------------------------------------- | ------------------------------------------------------------- | -------------------------------------- | ---------------------------- | ---------------------------------- | ---------------------------------- |
| eligible / blocked / deferred | outer_anchor / wrapper / thunk / dispatch_helper / substantive_body | `pending_function@address` | outermost_anchor / child_of_matched_boundary | entry_adjacent / dispatch_edge / callee / wrapper_edge / none | `pending_local_verification` or `none` | `pending_local_verification` | `incoming_refs=<n>; body_size=<n>` | metrics are secondary context only |

### Context Strings

| Address                      | Preview                      |
| ---------------------------- | ---------------------------- |
| `pending_local_verification` | `pending_local_verification` |

### External Symbol Snapshot

| Library                      | Symbol                       | Address                      |
| ---------------------------- | ---------------------------- | ---------------------------- |
| `pending_local_verification` | `pending_local_verification` | `pending_local_verification` |

## Rules

- This file is a reviewable export surface, not a mutation log.
- The primary review surface is frontier eligibility, not score order.
- Wrapper, thunk, and dispatch-helper rows may outrank a deeper substantive
  body when they are the current frontier boundary.
- Visible metrics stay broad enough to guide context, but they do not authorize
  a row by themselves.
- Runtime generation should write to a workspace copy under
  `.work/ghidra-artifacts/<target-id>/evidence-candidates.md`.

## Current Local Observation

This example file records the registered review surface. Local replay
validation is captured separately in `latest-version-validation.md`.
