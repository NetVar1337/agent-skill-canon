# Output Contract

The final output must at minimum include:

- The target request and parameter locations
- Which script and which function participate in generating it
- Evidence sources: request, call stack, breakpoints, runtime values
- Whether it can currently be reproduced stably
- If unfinished, which environment gap remains
